<div class="modal fade" id="contact-admin-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Contactar al administrador del sitio</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <%= form_for :contact_admin_mailer, url: contact_admin_mailer_index_path, method: :post, remote: true  do |f| %>
        <div class="modal-body">
            <div class="form-group">
                <%= f.label 'Nombre', class: 'control-label' %>
                <%= f.text_field :from, class: 'form-control', required: true %>
            </div>
            <div class="form-group">
                <%= f.label 'Correo electrónico', class: 'control-label' %>
                <%= f.email_field :contact_email, class: 'form-control', required: true %>
            </div>
            <div class="form-group">
                <%= f.label 'Número de contacto', class: 'control-label' %>
                <%= f.text_field :phone_number, class: 'form-control' %>
            </div>
            <div class="form-group">
                <%= f.label 'Mensaje' %>
                <%= f.text_area :body, class: 'form-control', required: true %>
            </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Cerrar</button>
          <%= f.submit 'Enviar', class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>