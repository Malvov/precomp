$('#modal-container').html('<%= j render "form", title: "Publicar Producto", product: @product %>');
$('#product-modal').on('shown.bs.modal', function() {
    $('form[data-validate]').enableClientSideValidations();
}).modal('show');